# Test data for the User table, for seeding the local Data Connect emulator.
# Run this against the local emulator (NOT production) via the Data Connect
# emulator's GraphQL explorer, or with the Firebase CLI:
#   firebase dataconnect:sql:migrate   (only needed if the schema changed)
#   Then execute this mutation from the emulator UI / VS Code extension.
mutation SeedUsers @auth(level: NO_ACCESS) {
  user_insert(data: { email: "thabo.mokoena@example.com", firstName: "Thabo", lastName: "Mokoena" })
  user2: user_insert(data: { email: "amahle.dlamini@example.com", firstName: "Amahle", lastName: "Dlamini" })
  user3: user_insert(data: { email: "sipho.nkosi@example.com", firstName: "Sipho", lastName: "Nkosi" })
  # Add as many consecutive mutations as your initial zero-state data requires
}