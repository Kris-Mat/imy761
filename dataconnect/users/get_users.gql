# Query used by backend servers (via the Data Connect Admin SDK) to list users.
# NO_ACCESS blocks all client-authenticated access; the Admin SDK bypasses @auth
# entirely, so this stays server-only, matching the SeedUsers mutation.
query GetUsers @auth(level: NO_ACCESS) {
  users {
    id
    email
    firstName
    lastName
  }
}
